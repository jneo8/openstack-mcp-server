apiVersion: v1
kind: Service
metadata:
  name: {{ include "openstack-mcp-server.fullname" . }}
  labels:
    {{- include "openstack-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "openstack-mcp-server.selectorLabels" . | nindent 4 }}
